<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | MGM Report Collector</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"/>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" href="/images/fevicon.jpeg">
</head>
<body class="d-flex flex-column min-vh-100">

    <header class="college-header py-3 border-bottom bg-light">
        <div class="container d-flex align-items-center">
            <!-- Logo on the left -->
            <img src="/images/fevicon.jpeg" alt="MGM Logo" class="college-logo me-3" style="height: 60px;">

            <!-- College name + subheading on the right -->
            <div>
                <h4 class="mb-0 fw-bold text-dark">M.G.M's College of Engineering, Nanded</h4>
                <h6 class="text-muted mb-0">Report submission website</h6>
            </div>
        </div>
    </header>

    <% if (faculty) { %>
        <%- include('navbar') %>
    <% } %>

    <main class="container my-4 flex-grow-1">
